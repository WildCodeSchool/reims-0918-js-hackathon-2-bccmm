import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: { main: "#005F82" },
    secondary: { main: "#EF914B" }
  },
  typography: {
    fontFamily: '"Product Sans", serif',
  },

});
